{
  "name": "metroclub",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/backend",
    "apps/admin-web"
  ],
  "scripts": {
    "dev:backend": "npm run start:dev --workspace=apps/backend",
    "dev:admin": "npm run dev --workspace=apps/admin-web",
    "build": "npm run build --workspace=apps/backend && npm run build --workspace=apps/admin-web",
    "lint": "npm run lint --workspace=apps/backend && npm run lint --workspace=apps/admin-web"
  }
}
